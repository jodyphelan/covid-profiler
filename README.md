# covid-profiler

## Install

```
conda install python=3.7 pdf2svg pymongo pyfastx plotly seqlogo redis redis-py tqdm biopython flask celery mongodb blast samtools bcftools minimap2 parallel mafft bwa snp-sites iqtree trimmomatic bedtools datamash
pip install flask_pymongo
```
